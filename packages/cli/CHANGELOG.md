# Keysight Scope CLI Changelog

## 0.1.0

- Adds the `keysight-scopes` console script and
  `python -m keysight_scope_cli.cli` module entry point.
- Public documentation now covers command usage, integration notes, JSON
  behavior, and automation safety.
